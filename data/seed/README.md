# data/seed

Synthetic demo dataset. Schema: [`docs/seed-data-spec.md`](../../docs/seed-data-spec.md).

One JSON file per location (e.g. `rampur.json`, `sundarpur.json`). Shared across all
services — coordinate changes with the team, don't edit another location's file without
flagging it.
